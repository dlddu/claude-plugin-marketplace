name: Test Push Workflow 2

on:
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run build step
        run: |
          echo "Running test push workflow 2..."
          sleep 3
          echo "Build step completed!"

  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run lint
        run: |
          echo "Running lint..."
          sleep 2
          echo "Lint completed!"
