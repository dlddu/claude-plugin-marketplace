name: Test Push Workflow 1

on:
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run test 1
        run: |
          echo "Running test push workflow 1..."
          sleep 5
          echo "Test 1 completed!"
